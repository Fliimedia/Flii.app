// Register every client here. Adding a client is two steps:
// 1. copy _template.json to <id>.json in this folder and fill it in
// 2. import it below (keep the with type json attribute) and add it to the map, keyed by its id
// Then add the client's pin hash to CLIENT_PINS in the hosting environment.
//
// No clients yet. Once the first one exists, the shape is:
//
//   import acme from "./acme.json" with { type: "json" };
//   export default { [acme.id]: acme };

export default {};
